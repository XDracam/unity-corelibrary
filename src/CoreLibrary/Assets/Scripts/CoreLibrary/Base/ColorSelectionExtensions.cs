using UnityEngine;

namespace CoreLibrary
{
    // ReSharper disable all InconsistentNaming

    /// <summary>
    /// Author: Cameron Reuschel
    /// <br/><br/>
    /// The methods in this class are GENERATED through the
    /// 'generateColorSelectionExtensions.sc' scala script
    /// in the '_CodeGenScripts' folder.
    /// </summary>
    public static class ColorSelectionExtensions
    {
        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrrr-values of the passed color in that order.
        /// </summary>
        public static Color rrrr(this Color col)
        {
            return new Color(col.r, col.r, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrrg-values of the passed color in that order.
        /// </summary>
        public static Color rrrg(this Color col)
        {
            return new Color(col.r, col.r, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrrb-values of the passed color in that order.
        /// </summary>
        public static Color rrrb(this Color col)
        {
            return new Color(col.r, col.r, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrra-values of the passed color in that order.
        /// </summary>
        public static Color rrra(this Color col)
        {
            return new Color(col.r, col.r, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrgr-values of the passed color in that order.
        /// </summary>
        public static Color rrgr(this Color col)
        {
            return new Color(col.r, col.r, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrgg-values of the passed color in that order.
        /// </summary>
        public static Color rrgg(this Color col)
        {
            return new Color(col.r, col.r, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrgb-values of the passed color in that order.
        /// </summary>
        public static Color rrgb(this Color col)
        {
            return new Color(col.r, col.r, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrga-values of the passed color in that order.
        /// </summary>
        public static Color rrga(this Color col)
        {
            return new Color(col.r, col.r, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrbr-values of the passed color in that order.
        /// </summary>
        public static Color rrbr(this Color col)
        {
            return new Color(col.r, col.r, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrbg-values of the passed color in that order.
        /// </summary>
        public static Color rrbg(this Color col)
        {
            return new Color(col.r, col.r, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrbb-values of the passed color in that order.
        /// </summary>
        public static Color rrbb(this Color col)
        {
            return new Color(col.r, col.r, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrba-values of the passed color in that order.
        /// </summary>
        public static Color rrba(this Color col)
        {
            return new Color(col.r, col.r, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrar-values of the passed color in that order.
        /// </summary>
        public static Color rrar(this Color col)
        {
            return new Color(col.r, col.r, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrag-values of the passed color in that order.
        /// </summary>
        public static Color rrag(this Color col)
        {
            return new Color(col.r, col.r, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rrab-values of the passed color in that order.
        /// </summary>
        public static Color rrab(this Color col)
        {
            return new Color(col.r, col.r, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rraa-values of the passed color in that order.
        /// </summary>
        public static Color rraa(this Color col)
        {
            return new Color(col.r, col.r, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgrr-values of the passed color in that order.
        /// </summary>
        public static Color rgrr(this Color col)
        {
            return new Color(col.r, col.g, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgrg-values of the passed color in that order.
        /// </summary>
        public static Color rgrg(this Color col)
        {
            return new Color(col.r, col.g, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgrb-values of the passed color in that order.
        /// </summary>
        public static Color rgrb(this Color col)
        {
            return new Color(col.r, col.g, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgra-values of the passed color in that order.
        /// </summary>
        public static Color rgra(this Color col)
        {
            return new Color(col.r, col.g, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rggr-values of the passed color in that order.
        /// </summary>
        public static Color rggr(this Color col)
        {
            return new Color(col.r, col.g, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rggg-values of the passed color in that order.
        /// </summary>
        public static Color rggg(this Color col)
        {
            return new Color(col.r, col.g, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rggb-values of the passed color in that order.
        /// </summary>
        public static Color rggb(this Color col)
        {
            return new Color(col.r, col.g, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgga-values of the passed color in that order.
        /// </summary>
        public static Color rgga(this Color col)
        {
            return new Color(col.r, col.g, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgbr-values of the passed color in that order.
        /// </summary>
        public static Color rgbr(this Color col)
        {
            return new Color(col.r, col.g, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgbg-values of the passed color in that order.
        /// </summary>
        public static Color rgbg(this Color col)
        {
            return new Color(col.r, col.g, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgbb-values of the passed color in that order.
        /// </summary>
        public static Color rgbb(this Color col)
        {
            return new Color(col.r, col.g, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgba-values of the passed color in that order.
        /// </summary>
        public static Color rgba(this Color col)
        {
            return new Color(col.r, col.g, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgar-values of the passed color in that order.
        /// </summary>
        public static Color rgar(this Color col)
        {
            return new Color(col.r, col.g, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgag-values of the passed color in that order.
        /// </summary>
        public static Color rgag(this Color col)
        {
            return new Color(col.r, col.g, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgab-values of the passed color in that order.
        /// </summary>
        public static Color rgab(this Color col)
        {
            return new Color(col.r, col.g, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rgaa-values of the passed color in that order.
        /// </summary>
        public static Color rgaa(this Color col)
        {
            return new Color(col.r, col.g, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbrr-values of the passed color in that order.
        /// </summary>
        public static Color rbrr(this Color col)
        {
            return new Color(col.r, col.b, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbrg-values of the passed color in that order.
        /// </summary>
        public static Color rbrg(this Color col)
        {
            return new Color(col.r, col.b, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbrb-values of the passed color in that order.
        /// </summary>
        public static Color rbrb(this Color col)
        {
            return new Color(col.r, col.b, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbra-values of the passed color in that order.
        /// </summary>
        public static Color rbra(this Color col)
        {
            return new Color(col.r, col.b, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbgr-values of the passed color in that order.
        /// </summary>
        public static Color rbgr(this Color col)
        {
            return new Color(col.r, col.b, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbgg-values of the passed color in that order.
        /// </summary>
        public static Color rbgg(this Color col)
        {
            return new Color(col.r, col.b, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbgb-values of the passed color in that order.
        /// </summary>
        public static Color rbgb(this Color col)
        {
            return new Color(col.r, col.b, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbga-values of the passed color in that order.
        /// </summary>
        public static Color rbga(this Color col)
        {
            return new Color(col.r, col.b, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbbr-values of the passed color in that order.
        /// </summary>
        public static Color rbbr(this Color col)
        {
            return new Color(col.r, col.b, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbbg-values of the passed color in that order.
        /// </summary>
        public static Color rbbg(this Color col)
        {
            return new Color(col.r, col.b, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbbb-values of the passed color in that order.
        /// </summary>
        public static Color rbbb(this Color col)
        {
            return new Color(col.r, col.b, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbba-values of the passed color in that order.
        /// </summary>
        public static Color rbba(this Color col)
        {
            return new Color(col.r, col.b, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbar-values of the passed color in that order.
        /// </summary>
        public static Color rbar(this Color col)
        {
            return new Color(col.r, col.b, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbag-values of the passed color in that order.
        /// </summary>
        public static Color rbag(this Color col)
        {
            return new Color(col.r, col.b, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbab-values of the passed color in that order.
        /// </summary>
        public static Color rbab(this Color col)
        {
            return new Color(col.r, col.b, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rbaa-values of the passed color in that order.
        /// </summary>
        public static Color rbaa(this Color col)
        {
            return new Color(col.r, col.b, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rarr-values of the passed color in that order.
        /// </summary>
        public static Color rarr(this Color col)
        {
            return new Color(col.r, col.a, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rarg-values of the passed color in that order.
        /// </summary>
        public static Color rarg(this Color col)
        {
            return new Color(col.r, col.a, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rarb-values of the passed color in that order.
        /// </summary>
        public static Color rarb(this Color col)
        {
            return new Color(col.r, col.a, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rara-values of the passed color in that order.
        /// </summary>
        public static Color rara(this Color col)
        {
            return new Color(col.r, col.a, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ragr-values of the passed color in that order.
        /// </summary>
        public static Color ragr(this Color col)
        {
            return new Color(col.r, col.a, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ragg-values of the passed color in that order.
        /// </summary>
        public static Color ragg(this Color col)
        {
            return new Color(col.r, col.a, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ragb-values of the passed color in that order.
        /// </summary>
        public static Color ragb(this Color col)
        {
            return new Color(col.r, col.a, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raga-values of the passed color in that order.
        /// </summary>
        public static Color raga(this Color col)
        {
            return new Color(col.r, col.a, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rabr-values of the passed color in that order.
        /// </summary>
        public static Color rabr(this Color col)
        {
            return new Color(col.r, col.a, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rabg-values of the passed color in that order.
        /// </summary>
        public static Color rabg(this Color col)
        {
            return new Color(col.r, col.a, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the rabb-values of the passed color in that order.
        /// </summary>
        public static Color rabb(this Color col)
        {
            return new Color(col.r, col.a, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raba-values of the passed color in that order.
        /// </summary>
        public static Color raba(this Color col)
        {
            return new Color(col.r, col.a, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raar-values of the passed color in that order.
        /// </summary>
        public static Color raar(this Color col)
        {
            return new Color(col.r, col.a, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raag-values of the passed color in that order.
        /// </summary>
        public static Color raag(this Color col)
        {
            return new Color(col.r, col.a, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raab-values of the passed color in that order.
        /// </summary>
        public static Color raab(this Color col)
        {
            return new Color(col.r, col.a, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the raaa-values of the passed color in that order.
        /// </summary>
        public static Color raaa(this Color col)
        {
            return new Color(col.r, col.a, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grrr-values of the passed color in that order.
        /// </summary>
        public static Color grrr(this Color col)
        {
            return new Color(col.g, col.r, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grrg-values of the passed color in that order.
        /// </summary>
        public static Color grrg(this Color col)
        {
            return new Color(col.g, col.r, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grrb-values of the passed color in that order.
        /// </summary>
        public static Color grrb(this Color col)
        {
            return new Color(col.g, col.r, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grra-values of the passed color in that order.
        /// </summary>
        public static Color grra(this Color col)
        {
            return new Color(col.g, col.r, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grgr-values of the passed color in that order.
        /// </summary>
        public static Color grgr(this Color col)
        {
            return new Color(col.g, col.r, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grgg-values of the passed color in that order.
        /// </summary>
        public static Color grgg(this Color col)
        {
            return new Color(col.g, col.r, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grgb-values of the passed color in that order.
        /// </summary>
        public static Color grgb(this Color col)
        {
            return new Color(col.g, col.r, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grga-values of the passed color in that order.
        /// </summary>
        public static Color grga(this Color col)
        {
            return new Color(col.g, col.r, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grbr-values of the passed color in that order.
        /// </summary>
        public static Color grbr(this Color col)
        {
            return new Color(col.g, col.r, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grbg-values of the passed color in that order.
        /// </summary>
        public static Color grbg(this Color col)
        {
            return new Color(col.g, col.r, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grbb-values of the passed color in that order.
        /// </summary>
        public static Color grbb(this Color col)
        {
            return new Color(col.g, col.r, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grba-values of the passed color in that order.
        /// </summary>
        public static Color grba(this Color col)
        {
            return new Color(col.g, col.r, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grar-values of the passed color in that order.
        /// </summary>
        public static Color grar(this Color col)
        {
            return new Color(col.g, col.r, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grag-values of the passed color in that order.
        /// </summary>
        public static Color grag(this Color col)
        {
            return new Color(col.g, col.r, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the grab-values of the passed color in that order.
        /// </summary>
        public static Color grab(this Color col)
        {
            return new Color(col.g, col.r, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the graa-values of the passed color in that order.
        /// </summary>
        public static Color graa(this Color col)
        {
            return new Color(col.g, col.r, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggrr-values of the passed color in that order.
        /// </summary>
        public static Color ggrr(this Color col)
        {
            return new Color(col.g, col.g, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggrg-values of the passed color in that order.
        /// </summary>
        public static Color ggrg(this Color col)
        {
            return new Color(col.g, col.g, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggrb-values of the passed color in that order.
        /// </summary>
        public static Color ggrb(this Color col)
        {
            return new Color(col.g, col.g, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggra-values of the passed color in that order.
        /// </summary>
        public static Color ggra(this Color col)
        {
            return new Color(col.g, col.g, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gggr-values of the passed color in that order.
        /// </summary>
        public static Color gggr(this Color col)
        {
            return new Color(col.g, col.g, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gggg-values of the passed color in that order.
        /// </summary>
        public static Color gggg(this Color col)
        {
            return new Color(col.g, col.g, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gggb-values of the passed color in that order.
        /// </summary>
        public static Color gggb(this Color col)
        {
            return new Color(col.g, col.g, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggga-values of the passed color in that order.
        /// </summary>
        public static Color ggga(this Color col)
        {
            return new Color(col.g, col.g, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggbr-values of the passed color in that order.
        /// </summary>
        public static Color ggbr(this Color col)
        {
            return new Color(col.g, col.g, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggbg-values of the passed color in that order.
        /// </summary>
        public static Color ggbg(this Color col)
        {
            return new Color(col.g, col.g, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggbb-values of the passed color in that order.
        /// </summary>
        public static Color ggbb(this Color col)
        {
            return new Color(col.g, col.g, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggba-values of the passed color in that order.
        /// </summary>
        public static Color ggba(this Color col)
        {
            return new Color(col.g, col.g, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggar-values of the passed color in that order.
        /// </summary>
        public static Color ggar(this Color col)
        {
            return new Color(col.g, col.g, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggag-values of the passed color in that order.
        /// </summary>
        public static Color ggag(this Color col)
        {
            return new Color(col.g, col.g, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggab-values of the passed color in that order.
        /// </summary>
        public static Color ggab(this Color col)
        {
            return new Color(col.g, col.g, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the ggaa-values of the passed color in that order.
        /// </summary>
        public static Color ggaa(this Color col)
        {
            return new Color(col.g, col.g, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbrr-values of the passed color in that order.
        /// </summary>
        public static Color gbrr(this Color col)
        {
            return new Color(col.g, col.b, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbrg-values of the passed color in that order.
        /// </summary>
        public static Color gbrg(this Color col)
        {
            return new Color(col.g, col.b, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbrb-values of the passed color in that order.
        /// </summary>
        public static Color gbrb(this Color col)
        {
            return new Color(col.g, col.b, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbra-values of the passed color in that order.
        /// </summary>
        public static Color gbra(this Color col)
        {
            return new Color(col.g, col.b, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbgr-values of the passed color in that order.
        /// </summary>
        public static Color gbgr(this Color col)
        {
            return new Color(col.g, col.b, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbgg-values of the passed color in that order.
        /// </summary>
        public static Color gbgg(this Color col)
        {
            return new Color(col.g, col.b, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbgb-values of the passed color in that order.
        /// </summary>
        public static Color gbgb(this Color col)
        {
            return new Color(col.g, col.b, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbga-values of the passed color in that order.
        /// </summary>
        public static Color gbga(this Color col)
        {
            return new Color(col.g, col.b, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbbr-values of the passed color in that order.
        /// </summary>
        public static Color gbbr(this Color col)
        {
            return new Color(col.g, col.b, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbbg-values of the passed color in that order.
        /// </summary>
        public static Color gbbg(this Color col)
        {
            return new Color(col.g, col.b, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbbb-values of the passed color in that order.
        /// </summary>
        public static Color gbbb(this Color col)
        {
            return new Color(col.g, col.b, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbba-values of the passed color in that order.
        /// </summary>
        public static Color gbba(this Color col)
        {
            return new Color(col.g, col.b, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbar-values of the passed color in that order.
        /// </summary>
        public static Color gbar(this Color col)
        {
            return new Color(col.g, col.b, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbag-values of the passed color in that order.
        /// </summary>
        public static Color gbag(this Color col)
        {
            return new Color(col.g, col.b, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbab-values of the passed color in that order.
        /// </summary>
        public static Color gbab(this Color col)
        {
            return new Color(col.g, col.b, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gbaa-values of the passed color in that order.
        /// </summary>
        public static Color gbaa(this Color col)
        {
            return new Color(col.g, col.b, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the garr-values of the passed color in that order.
        /// </summary>
        public static Color garr(this Color col)
        {
            return new Color(col.g, col.a, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the garg-values of the passed color in that order.
        /// </summary>
        public static Color garg(this Color col)
        {
            return new Color(col.g, col.a, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the garb-values of the passed color in that order.
        /// </summary>
        public static Color garb(this Color col)
        {
            return new Color(col.g, col.a, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gara-values of the passed color in that order.
        /// </summary>
        public static Color gara(this Color col)
        {
            return new Color(col.g, col.a, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gagr-values of the passed color in that order.
        /// </summary>
        public static Color gagr(this Color col)
        {
            return new Color(col.g, col.a, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gagg-values of the passed color in that order.
        /// </summary>
        public static Color gagg(this Color col)
        {
            return new Color(col.g, col.a, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gagb-values of the passed color in that order.
        /// </summary>
        public static Color gagb(this Color col)
        {
            return new Color(col.g, col.a, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaga-values of the passed color in that order.
        /// </summary>
        public static Color gaga(this Color col)
        {
            return new Color(col.g, col.a, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gabr-values of the passed color in that order.
        /// </summary>
        public static Color gabr(this Color col)
        {
            return new Color(col.g, col.a, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gabg-values of the passed color in that order.
        /// </summary>
        public static Color gabg(this Color col)
        {
            return new Color(col.g, col.a, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gabb-values of the passed color in that order.
        /// </summary>
        public static Color gabb(this Color col)
        {
            return new Color(col.g, col.a, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaba-values of the passed color in that order.
        /// </summary>
        public static Color gaba(this Color col)
        {
            return new Color(col.g, col.a, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaar-values of the passed color in that order.
        /// </summary>
        public static Color gaar(this Color col)
        {
            return new Color(col.g, col.a, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaag-values of the passed color in that order.
        /// </summary>
        public static Color gaag(this Color col)
        {
            return new Color(col.g, col.a, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaab-values of the passed color in that order.
        /// </summary>
        public static Color gaab(this Color col)
        {
            return new Color(col.g, col.a, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the gaaa-values of the passed color in that order.
        /// </summary>
        public static Color gaaa(this Color col)
        {
            return new Color(col.g, col.a, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brrr-values of the passed color in that order.
        /// </summary>
        public static Color brrr(this Color col)
        {
            return new Color(col.b, col.r, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brrg-values of the passed color in that order.
        /// </summary>
        public static Color brrg(this Color col)
        {
            return new Color(col.b, col.r, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brrb-values of the passed color in that order.
        /// </summary>
        public static Color brrb(this Color col)
        {
            return new Color(col.b, col.r, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brra-values of the passed color in that order.
        /// </summary>
        public static Color brra(this Color col)
        {
            return new Color(col.b, col.r, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brgr-values of the passed color in that order.
        /// </summary>
        public static Color brgr(this Color col)
        {
            return new Color(col.b, col.r, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brgg-values of the passed color in that order.
        /// </summary>
        public static Color brgg(this Color col)
        {
            return new Color(col.b, col.r, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brgb-values of the passed color in that order.
        /// </summary>
        public static Color brgb(this Color col)
        {
            return new Color(col.b, col.r, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brga-values of the passed color in that order.
        /// </summary>
        public static Color brga(this Color col)
        {
            return new Color(col.b, col.r, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brbr-values of the passed color in that order.
        /// </summary>
        public static Color brbr(this Color col)
        {
            return new Color(col.b, col.r, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brbg-values of the passed color in that order.
        /// </summary>
        public static Color brbg(this Color col)
        {
            return new Color(col.b, col.r, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brbb-values of the passed color in that order.
        /// </summary>
        public static Color brbb(this Color col)
        {
            return new Color(col.b, col.r, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brba-values of the passed color in that order.
        /// </summary>
        public static Color brba(this Color col)
        {
            return new Color(col.b, col.r, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brar-values of the passed color in that order.
        /// </summary>
        public static Color brar(this Color col)
        {
            return new Color(col.b, col.r, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brag-values of the passed color in that order.
        /// </summary>
        public static Color brag(this Color col)
        {
            return new Color(col.b, col.r, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the brab-values of the passed color in that order.
        /// </summary>
        public static Color brab(this Color col)
        {
            return new Color(col.b, col.r, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the braa-values of the passed color in that order.
        /// </summary>
        public static Color braa(this Color col)
        {
            return new Color(col.b, col.r, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgrr-values of the passed color in that order.
        /// </summary>
        public static Color bgrr(this Color col)
        {
            return new Color(col.b, col.g, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgrg-values of the passed color in that order.
        /// </summary>
        public static Color bgrg(this Color col)
        {
            return new Color(col.b, col.g, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgrb-values of the passed color in that order.
        /// </summary>
        public static Color bgrb(this Color col)
        {
            return new Color(col.b, col.g, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgra-values of the passed color in that order.
        /// </summary>
        public static Color bgra(this Color col)
        {
            return new Color(col.b, col.g, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bggr-values of the passed color in that order.
        /// </summary>
        public static Color bggr(this Color col)
        {
            return new Color(col.b, col.g, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bggg-values of the passed color in that order.
        /// </summary>
        public static Color bggg(this Color col)
        {
            return new Color(col.b, col.g, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bggb-values of the passed color in that order.
        /// </summary>
        public static Color bggb(this Color col)
        {
            return new Color(col.b, col.g, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgga-values of the passed color in that order.
        /// </summary>
        public static Color bgga(this Color col)
        {
            return new Color(col.b, col.g, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgbr-values of the passed color in that order.
        /// </summary>
        public static Color bgbr(this Color col)
        {
            return new Color(col.b, col.g, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgbg-values of the passed color in that order.
        /// </summary>
        public static Color bgbg(this Color col)
        {
            return new Color(col.b, col.g, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgbb-values of the passed color in that order.
        /// </summary>
        public static Color bgbb(this Color col)
        {
            return new Color(col.b, col.g, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgba-values of the passed color in that order.
        /// </summary>
        public static Color bgba(this Color col)
        {
            return new Color(col.b, col.g, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgar-values of the passed color in that order.
        /// </summary>
        public static Color bgar(this Color col)
        {
            return new Color(col.b, col.g, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgag-values of the passed color in that order.
        /// </summary>
        public static Color bgag(this Color col)
        {
            return new Color(col.b, col.g, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgab-values of the passed color in that order.
        /// </summary>
        public static Color bgab(this Color col)
        {
            return new Color(col.b, col.g, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bgaa-values of the passed color in that order.
        /// </summary>
        public static Color bgaa(this Color col)
        {
            return new Color(col.b, col.g, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbrr-values of the passed color in that order.
        /// </summary>
        public static Color bbrr(this Color col)
        {
            return new Color(col.b, col.b, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbrg-values of the passed color in that order.
        /// </summary>
        public static Color bbrg(this Color col)
        {
            return new Color(col.b, col.b, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbrb-values of the passed color in that order.
        /// </summary>
        public static Color bbrb(this Color col)
        {
            return new Color(col.b, col.b, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbra-values of the passed color in that order.
        /// </summary>
        public static Color bbra(this Color col)
        {
            return new Color(col.b, col.b, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbgr-values of the passed color in that order.
        /// </summary>
        public static Color bbgr(this Color col)
        {
            return new Color(col.b, col.b, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbgg-values of the passed color in that order.
        /// </summary>
        public static Color bbgg(this Color col)
        {
            return new Color(col.b, col.b, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbgb-values of the passed color in that order.
        /// </summary>
        public static Color bbgb(this Color col)
        {
            return new Color(col.b, col.b, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbga-values of the passed color in that order.
        /// </summary>
        public static Color bbga(this Color col)
        {
            return new Color(col.b, col.b, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbbr-values of the passed color in that order.
        /// </summary>
        public static Color bbbr(this Color col)
        {
            return new Color(col.b, col.b, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbbg-values of the passed color in that order.
        /// </summary>
        public static Color bbbg(this Color col)
        {
            return new Color(col.b, col.b, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbbb-values of the passed color in that order.
        /// </summary>
        public static Color bbbb(this Color col)
        {
            return new Color(col.b, col.b, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbba-values of the passed color in that order.
        /// </summary>
        public static Color bbba(this Color col)
        {
            return new Color(col.b, col.b, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbar-values of the passed color in that order.
        /// </summary>
        public static Color bbar(this Color col)
        {
            return new Color(col.b, col.b, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbag-values of the passed color in that order.
        /// </summary>
        public static Color bbag(this Color col)
        {
            return new Color(col.b, col.b, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbab-values of the passed color in that order.
        /// </summary>
        public static Color bbab(this Color col)
        {
            return new Color(col.b, col.b, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bbaa-values of the passed color in that order.
        /// </summary>
        public static Color bbaa(this Color col)
        {
            return new Color(col.b, col.b, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the barr-values of the passed color in that order.
        /// </summary>
        public static Color barr(this Color col)
        {
            return new Color(col.b, col.a, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the barg-values of the passed color in that order.
        /// </summary>
        public static Color barg(this Color col)
        {
            return new Color(col.b, col.a, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the barb-values of the passed color in that order.
        /// </summary>
        public static Color barb(this Color col)
        {
            return new Color(col.b, col.a, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bara-values of the passed color in that order.
        /// </summary>
        public static Color bara(this Color col)
        {
            return new Color(col.b, col.a, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bagr-values of the passed color in that order.
        /// </summary>
        public static Color bagr(this Color col)
        {
            return new Color(col.b, col.a, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bagg-values of the passed color in that order.
        /// </summary>
        public static Color bagg(this Color col)
        {
            return new Color(col.b, col.a, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the bagb-values of the passed color in that order.
        /// </summary>
        public static Color bagb(this Color col)
        {
            return new Color(col.b, col.a, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baga-values of the passed color in that order.
        /// </summary>
        public static Color baga(this Color col)
        {
            return new Color(col.b, col.a, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the babr-values of the passed color in that order.
        /// </summary>
        public static Color babr(this Color col)
        {
            return new Color(col.b, col.a, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the babg-values of the passed color in that order.
        /// </summary>
        public static Color babg(this Color col)
        {
            return new Color(col.b, col.a, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the babb-values of the passed color in that order.
        /// </summary>
        public static Color babb(this Color col)
        {
            return new Color(col.b, col.a, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baba-values of the passed color in that order.
        /// </summary>
        public static Color baba(this Color col)
        {
            return new Color(col.b, col.a, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baar-values of the passed color in that order.
        /// </summary>
        public static Color baar(this Color col)
        {
            return new Color(col.b, col.a, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baag-values of the passed color in that order.
        /// </summary>
        public static Color baag(this Color col)
        {
            return new Color(col.b, col.a, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baab-values of the passed color in that order.
        /// </summary>
        public static Color baab(this Color col)
        {
            return new Color(col.b, col.a, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the baaa-values of the passed color in that order.
        /// </summary>
        public static Color baaa(this Color col)
        {
            return new Color(col.b, col.a, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arrr-values of the passed color in that order.
        /// </summary>
        public static Color arrr(this Color col)
        {
            return new Color(col.a, col.r, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arrg-values of the passed color in that order.
        /// </summary>
        public static Color arrg(this Color col)
        {
            return new Color(col.a, col.r, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arrb-values of the passed color in that order.
        /// </summary>
        public static Color arrb(this Color col)
        {
            return new Color(col.a, col.r, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arra-values of the passed color in that order.
        /// </summary>
        public static Color arra(this Color col)
        {
            return new Color(col.a, col.r, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the argr-values of the passed color in that order.
        /// </summary>
        public static Color argr(this Color col)
        {
            return new Color(col.a, col.r, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the argg-values of the passed color in that order.
        /// </summary>
        public static Color argg(this Color col)
        {
            return new Color(col.a, col.r, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the argb-values of the passed color in that order.
        /// </summary>
        public static Color argb(this Color col)
        {
            return new Color(col.a, col.r, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arga-values of the passed color in that order.
        /// </summary>
        public static Color arga(this Color col)
        {
            return new Color(col.a, col.r, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arbr-values of the passed color in that order.
        /// </summary>
        public static Color arbr(this Color col)
        {
            return new Color(col.a, col.r, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arbg-values of the passed color in that order.
        /// </summary>
        public static Color arbg(this Color col)
        {
            return new Color(col.a, col.r, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arbb-values of the passed color in that order.
        /// </summary>
        public static Color arbb(this Color col)
        {
            return new Color(col.a, col.r, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arba-values of the passed color in that order.
        /// </summary>
        public static Color arba(this Color col)
        {
            return new Color(col.a, col.r, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arar-values of the passed color in that order.
        /// </summary>
        public static Color arar(this Color col)
        {
            return new Color(col.a, col.r, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arag-values of the passed color in that order.
        /// </summary>
        public static Color arag(this Color col)
        {
            return new Color(col.a, col.r, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the arab-values of the passed color in that order.
        /// </summary>
        public static Color arab(this Color col)
        {
            return new Color(col.a, col.r, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the araa-values of the passed color in that order.
        /// </summary>
        public static Color araa(this Color col)
        {
            return new Color(col.a, col.r, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agrr-values of the passed color in that order.
        /// </summary>
        public static Color agrr(this Color col)
        {
            return new Color(col.a, col.g, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agrg-values of the passed color in that order.
        /// </summary>
        public static Color agrg(this Color col)
        {
            return new Color(col.a, col.g, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agrb-values of the passed color in that order.
        /// </summary>
        public static Color agrb(this Color col)
        {
            return new Color(col.a, col.g, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agra-values of the passed color in that order.
        /// </summary>
        public static Color agra(this Color col)
        {
            return new Color(col.a, col.g, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aggr-values of the passed color in that order.
        /// </summary>
        public static Color aggr(this Color col)
        {
            return new Color(col.a, col.g, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aggg-values of the passed color in that order.
        /// </summary>
        public static Color aggg(this Color col)
        {
            return new Color(col.a, col.g, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aggb-values of the passed color in that order.
        /// </summary>
        public static Color aggb(this Color col)
        {
            return new Color(col.a, col.g, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agga-values of the passed color in that order.
        /// </summary>
        public static Color agga(this Color col)
        {
            return new Color(col.a, col.g, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agbr-values of the passed color in that order.
        /// </summary>
        public static Color agbr(this Color col)
        {
            return new Color(col.a, col.g, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agbg-values of the passed color in that order.
        /// </summary>
        public static Color agbg(this Color col)
        {
            return new Color(col.a, col.g, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agbb-values of the passed color in that order.
        /// </summary>
        public static Color agbb(this Color col)
        {
            return new Color(col.a, col.g, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agba-values of the passed color in that order.
        /// </summary>
        public static Color agba(this Color col)
        {
            return new Color(col.a, col.g, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agar-values of the passed color in that order.
        /// </summary>
        public static Color agar(this Color col)
        {
            return new Color(col.a, col.g, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agag-values of the passed color in that order.
        /// </summary>
        public static Color agag(this Color col)
        {
            return new Color(col.a, col.g, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agab-values of the passed color in that order.
        /// </summary>
        public static Color agab(this Color col)
        {
            return new Color(col.a, col.g, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the agaa-values of the passed color in that order.
        /// </summary>
        public static Color agaa(this Color col)
        {
            return new Color(col.a, col.g, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abrr-values of the passed color in that order.
        /// </summary>
        public static Color abrr(this Color col)
        {
            return new Color(col.a, col.b, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abrg-values of the passed color in that order.
        /// </summary>
        public static Color abrg(this Color col)
        {
            return new Color(col.a, col.b, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abrb-values of the passed color in that order.
        /// </summary>
        public static Color abrb(this Color col)
        {
            return new Color(col.a, col.b, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abra-values of the passed color in that order.
        /// </summary>
        public static Color abra(this Color col)
        {
            return new Color(col.a, col.b, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abgr-values of the passed color in that order.
        /// </summary>
        public static Color abgr(this Color col)
        {
            return new Color(col.a, col.b, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abgg-values of the passed color in that order.
        /// </summary>
        public static Color abgg(this Color col)
        {
            return new Color(col.a, col.b, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abgb-values of the passed color in that order.
        /// </summary>
        public static Color abgb(this Color col)
        {
            return new Color(col.a, col.b, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abga-values of the passed color in that order.
        /// </summary>
        public static Color abga(this Color col)
        {
            return new Color(col.a, col.b, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abbr-values of the passed color in that order.
        /// </summary>
        public static Color abbr(this Color col)
        {
            return new Color(col.a, col.b, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abbg-values of the passed color in that order.
        /// </summary>
        public static Color abbg(this Color col)
        {
            return new Color(col.a, col.b, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abbb-values of the passed color in that order.
        /// </summary>
        public static Color abbb(this Color col)
        {
            return new Color(col.a, col.b, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abba-values of the passed color in that order.
        /// </summary>
        public static Color abba(this Color col)
        {
            return new Color(col.a, col.b, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abar-values of the passed color in that order.
        /// </summary>
        public static Color abar(this Color col)
        {
            return new Color(col.a, col.b, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abag-values of the passed color in that order.
        /// </summary>
        public static Color abag(this Color col)
        {
            return new Color(col.a, col.b, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abab-values of the passed color in that order.
        /// </summary>
        public static Color abab(this Color col)
        {
            return new Color(col.a, col.b, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the abaa-values of the passed color in that order.
        /// </summary>
        public static Color abaa(this Color col)
        {
            return new Color(col.a, col.b, col.a, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aarr-values of the passed color in that order.
        /// </summary>
        public static Color aarr(this Color col)
        {
            return new Color(col.a, col.a, col.r, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aarg-values of the passed color in that order.
        /// </summary>
        public static Color aarg(this Color col)
        {
            return new Color(col.a, col.a, col.r, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aarb-values of the passed color in that order.
        /// </summary>
        public static Color aarb(this Color col)
        {
            return new Color(col.a, col.a, col.r, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aara-values of the passed color in that order.
        /// </summary>
        public static Color aara(this Color col)
        {
            return new Color(col.a, col.a, col.r, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aagr-values of the passed color in that order.
        /// </summary>
        public static Color aagr(this Color col)
        {
            return new Color(col.a, col.a, col.g, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aagg-values of the passed color in that order.
        /// </summary>
        public static Color aagg(this Color col)
        {
            return new Color(col.a, col.a, col.g, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aagb-values of the passed color in that order.
        /// </summary>
        public static Color aagb(this Color col)
        {
            return new Color(col.a, col.a, col.g, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaga-values of the passed color in that order.
        /// </summary>
        public static Color aaga(this Color col)
        {
            return new Color(col.a, col.a, col.g, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aabr-values of the passed color in that order.
        /// </summary>
        public static Color aabr(this Color col)
        {
            return new Color(col.a, col.a, col.b, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aabg-values of the passed color in that order.
        /// </summary>
        public static Color aabg(this Color col)
        {
            return new Color(col.a, col.a, col.b, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aabb-values of the passed color in that order.
        /// </summary>
        public static Color aabb(this Color col)
        {
            return new Color(col.a, col.a, col.b, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaba-values of the passed color in that order.
        /// </summary>
        public static Color aaba(this Color col)
        {
            return new Color(col.a, col.a, col.b, col.a);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaar-values of the passed color in that order.
        /// </summary>
        public static Color aaar(this Color col)
        {
            return new Color(col.a, col.a, col.a, col.r);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaag-values of the passed color in that order.
        /// </summary>
        public static Color aaag(this Color col)
        {
            return new Color(col.a, col.a, col.a, col.g);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaab-values of the passed color in that order.
        /// </summary>
        public static Color aaab(this Color col)
        {
            return new Color(col.a, col.a, col.a, col.b);
        }

        /// <summary> 
        /// Returns a new Color with it's values set to 
        /// the aaaa-values of the passed color in that order.
        /// </summary>
        public static Color aaaa(this Color col)
        {
            return new Color(col.a, col.a, col.a, col.a);
        }
    }
}